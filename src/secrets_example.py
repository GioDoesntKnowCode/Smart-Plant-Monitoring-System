# Copy this file to `secrets.py` and fill in real values.
# `secrets.py` is gitignored.

WIFI_SSID = "your-ssid"
WIFI_PASSWORD = "your-password"

# Firebase Realtime Database URL (from the Firebase console).
# Note the region subdomain — yours may be `firebaseio.com` or `europe-west1.firebasedatabase.app`.
FIREBASE_DB_URL = "https://your-project-default-rtdb.europe-west1.firebasedatabase.app"

# Easiest auth on a microcontroller: a database secret.
# Firebase console → Project settings → Service accounts → Database secrets.
FIREBASE_AUTH = "your-database-secret"
